services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=db_users'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=abdelilah'
    ports:
      - '5432:5432'

